spring.application.name=CourseFlow
server.port=${SERVER_PORT:8080}
spring.profiles.default=standalone

# Local-only secrets (DB credentials, etc.) live in a gitignored .env.properties
# file at the backend module root; see README/.env.properties.example.
spring.config.import=optional:file:.env.properties

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${CLERK_JWKS_URL:https://caring-pangolin-516.clerk.accounts.dev/.well-known/jwks.json}
courseflow.app-base-url=${APP_BASE_URL:http://localhost:5173}
courseflow.payment.omise.public-key=${OMISE_PUBLIC_KEY:}
courseflow.payment.omise.secret-key=${OMISE_SECRET_KEY:}
